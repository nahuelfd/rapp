import {memo} from 'react';

const Footer = () => {
    return (
        <div className='divf'>
            <h1 className='foot'>Librerías Del Sur</h1>
        </div>
    );
}

export default memo(Footer);
